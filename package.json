{
  "name": "market-atlas",
  "version": "3.2.0",
  "private": true,
  "description": "CoinGecko market dashboard",
  "engines": {
    "node": ">=22.9"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env scripts/local.cjs",
    "dev": "node --env-file-if-exists=.env --watch scripts/local.cjs",
    "test": "node --test test/*.test.js"
  }
}
